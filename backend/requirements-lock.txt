# Fully resolved lock file (direct + transitive dependencies, exact
# versions) -- generated via `pip freeze` against a known-good,
# 119-tests-passing environment. requirements.txt stays the
# human-authored direct-dependency list for local dev; CI installs
# from THIS file instead, so a rebuild months from now can't silently
# pull a different transitive version of e.g. starlette or anyio.
#
# Regenerate with:
#   pip install -r requirements.txt && pip freeze > requirements-lock.txt
annotated-types==0.8.0
anyio==4.15.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
fastapi==0.115.0
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httpx==0.27.2
idna==3.19
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pydantic==2.13.5
pydantic_core==2.46.5
pytest==8.3.3
python-dotenv==1.0.1
requests==2.32.3
sniffio==1.3.1
SQLAlchemy==2.0.52
sqlmodel==0.0.42
starlette==0.38.6
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.30.6
